apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-typescript-threat-fixture
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-typescript-threat-fixture
  template:
    metadata:
      labels:
        app: node-typescript-threat-fixture
    spec:
      hostNetwork: true
      containers:
        - name: app
          image: node-typescript-threat-fixture:latest
          env:
            - name: ADMIN_BOOTSTRAP_TOKEN
              value: node-admin-demo-token
            - name: JWT_SIGNING_KEY
              value: node-fixture-jwt-key-too-short
          securityContext:
            privileged: true
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: false
            runAsUser: 0
          volumeMounts:
            - name: docker-sock
              mountPath: /var/run/docker.sock
      volumes:
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock

